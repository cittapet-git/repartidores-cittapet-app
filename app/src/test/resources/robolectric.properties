# Robolectric unit tests run against a bare Application. The real DriverApp calls
# FirebaseCrashlytics.getInstance() in onCreate(), which throws without a FirebaseApp
# (no google-services processing in the JVM unit-test classpath).
application=android.app.Application
